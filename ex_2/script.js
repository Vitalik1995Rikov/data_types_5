function sum(a, b) {
    a = +prompt('Введи первое число', 0);
    b = +prompt('Введи второе число', 0);
    return a + b;
}

alert(sum()); // создал функцию 
// принимает два числа, возвращает их сумму
